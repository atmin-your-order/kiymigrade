// scripts/migrate-to-supabase.js\n\nconst fs = require('fs');\nconst { createClient } = require('@supabase/supabase-js');\n\n// Initialize Supabase client\nconst supabase = createClient('https://your-supabase-url.supabase.co', 'your-supabase-anon-key');\n\n// Function to read JSON files and insert data into Supabase\nasync function migrateData() {\n    const files = ['users.json', 'products.json', 'database.json', 'panels.json', 'visitors.json'];\n    const tables = ['users', 'products', 'database', 'panels', 'visitors'];\n\n    for (let i = 0; i < files.length; i++) {\n        const file = files[i];\n        const table = tables[i];\n        \n        // Read JSON file\n        const data = JSON.parse(fs.readFileSync(file, 'utf-8'));\n\n        // Insert data into Supabase\n        const { data: insertData, error } = await supabase.from(table).insert(data);\n        if (error) {\n            console.error(`Error inserting data into ${table}:`, error);\n        } else {\n            console.log(`Data inserted into ${table}:`, insertData);\n        }\n    }\n}\n\nmigrateData().catch(console.error);\n